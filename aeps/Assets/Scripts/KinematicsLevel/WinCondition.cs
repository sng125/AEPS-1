using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using UnityEditor;
//using UnityEngine.TMPro;

public class WinCondition : MonoBehaviour
{
	/*
	public TextMeshProUGUI winTextObject;
	private Rigidbody rb;
	private bool hitOnce = true;
	
	
	
    // Start is called before the first frame update
    void Start()
    {
		rb = GetComponent<Rigidbody>();
        winTextObject.text = "";
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        //OnTriggerEnter(Collider2D);
    }
	
	private void OnTriggerEnter (Collider col)
	{
		if (col.gameObject.tag == "Ground" && hitOnce)
		{
			winTextObject.text = "You Lose";
			winTextObject.color = new Color(255, 0, 0, 255);
			hitOnce = false;
		}
		if (col.gameObject.tag == "Target" && hitOnce)
		{
			winTextObject.text = "You Win";
			winTextObject.color = new Color(1, 102, 0, 255);
			hitOnce = false;
		}
	}
	*/
	
}
